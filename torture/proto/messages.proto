syntax = "proto3";
option go_package = "proto/";

// this file defines the proto definitions of the messages that are used in torture replica

message Message {
  int32 operation = 1;
  bool on = 2;
  repeated int32 int_params = 3;
  repeated string str_params = 4;
}